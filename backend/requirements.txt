# CIRS Backend Dependencies
# Tested: Python 3.11, 3.12, 3.13

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Authentication
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6
# Note: Using hashlib (built-in) for PIN hashing instead of bcrypt for Python 3.13 compatibility

# Database (using built-in sqlite3)
# No additional packages needed for SQLite

# Utilities
python-dateutil>=2.8.2
pydantic>=2.5.0
pydantic-settings>=2.1.0

# QR Code generation
qrcode[pil]>=7.4.2
Pillow>=10.1.0  # Use >= for Python 3.13 compatibility

# CORS
# (included in FastAPI)

# xIRS Secure Data Exchange Protocol v2.0
# Encryption: Curve25519 + XSalsa20-Poly1305
# Signing: Ed25519
pynacl>=1.5.0
